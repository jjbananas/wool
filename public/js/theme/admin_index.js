jQuery(function() {	var img = $("img");	var baseSrc = img.attr("src");		$("input[type=color]").each(function() {		var input = $(this);		input.hide();				var container = $("<div>").addClass("colorPicker");		var target = $("<div>").addClass("inner").css("backgroundColor", input.val());				container.append(target);		input.after(container);				container.ColorPicker({			color: input.val(),			onChange: function(hsb, hex, rgb) {				input.val('#' + hex);				target.css("backgroundColor", '#' + hex);			},			onHide: function() {				img.attr("src", baseSrc + "?" + $("form").serialize());			}		});	});});